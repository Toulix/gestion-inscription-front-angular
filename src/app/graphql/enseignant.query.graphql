query enseignantProfile($enseignantId: String!) {
  enseignant(enseignantId: $enseignantId) {
    id
    nom
    prenom
    titre
    tel
    email
    image
    sexe
  }
}

query enseignants {
  enseignants {
    id
    nom
    prenom
    image
  }
}
